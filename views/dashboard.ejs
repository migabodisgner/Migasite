
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/css/dashboard.css">
    <style>
        body { margin:0; font-family: Arial, Helvetica, sans-serif; background:#f4f6f8; }
        .sidebar { width:240px; height:100vh; position:fixed; left:0; top:0; background:#1e293b; color:#fff; }
        .sidebar h2 { text-align:center; padding:20px 10px; border-bottom:1px solid #334155; }
        .sidebar a { display:block; padding:14px 20px; color:#cbd5e1; text-decoration:none; }
        .sidebar a:hover { background:#334155; color:#fff; }
        .main { margin-left:240px; padding:20px; }
        .topbar { background:#fff; padding:15px 20px; display:flex; justify-content:space-between; align-items:center; box-shadow:0 2px 4px rgba(0,0,0,.1); }
        .admin-info { font-weight:bold; }
        .cards { display:grid; grid-template-columns:repeat(auto-fit,minmax(200px,1fr)); gap:20px; margin-top:20px; }
        .card { background:#fff; padding:20px; border-radius:8px; box-shadow:0 2px 4px rgba(0,0,0,.1); }
        table { width:100%; border-collapse:collapse; margin-top:20px; background:#fff; }
        th, td { padding:12px; border-bottom:1px solid #e5e7eb; text-align:left; }
        th { background:#f1f5f9; }
        .btn { padding:6px 12px; border:none; border-radius:4px; cursor:pointer; }
        .btn-delete { background:#dc2626; color:#fff; }
    </style>
</head>
<body>

<div class="sidebar">
    <h2>Admin Panel</h2>
    <a href="/dashboard">Dashboard</a>
    <a href="#">Users</a>
    <a href="#">Settings</a>
    <a href="/">Logout</a>
</div>

<div class="main">
    <div class="topbar">
        <div class="admin-info">
            Admin: <%= admin.fullname %><br>
            <small><%= admin.email %></small>
        </div>
        <div>Welcome 👋</div>
    </div>

    <div class="cards">
        <div class="card">
            <h3>Total Users</h3>
            <p><%= users.length %></p>
        </div>
        <div class="card">
            <h3>Admins</h3>
            <p><%= adminsCount %></p>
        </div>
    </div>

    <h3>All Users</h3>
    <table>
        <thead>
            <tr>
                <th>Full Name</th>
                <th>Email</th>
                <th>Role</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% users.forEach(user => { %>
                <tr>
                    <td><%= user.fullname %></td>
                    <td><%= user.email %></td>
                    <td><%= user.role %></td>
                    <td>
                        <button class="btn btn-delete">Delete</button>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>

</body>
</html>

```
